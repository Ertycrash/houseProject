import {createSlice} from "@reduxjs/toolkit";

    const mainSlice = createSlice ({
        name: 'mainSlice',
        initialState: {
            main: []
        },
        reducers: {

        }
    })

    export default mainSlice.reducer